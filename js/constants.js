/**
 * Created by sashaz on 12.09.16.
 */

var GAME_CANVAS_WIDTH = window.innerWidth,
    GAME_CANVAS_HEIGHT = window.innerHeight,
    TREE_HEIGHT = 116,
    PLAYER_WIDTH = 32,
    PLAYER_HEIGHT = 33,
    PLAYER_GRAVITY = 600,
    PLAYER_VELOCITY = 180,
    PLAYER_JUMP_VELOCITY = -300,
    BLOCK_SIZE = 41,

    BOX_BOTTOM = GAME_CANVAS_HEIGHT - 38;